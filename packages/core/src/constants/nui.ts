/**
 * The ID of the NUI instance that is created by the platform driver. This ID is used to identify the NUI instance when sending messages to it or receiving messages from it.
 * Platforms like FiveM expose a single-instance NUI per resource, and this ID is used to ensure that messages are routed to the correct NUI instance.
 */
export const NUI_ID = 'aurora:nui';
