{
    "$schema": "https://turborepo.org/schema.json",
    "tasks": {
        "build": {
            "dependsOn": ["^build"],
            "outputs": ["dist/**"]
        },
        "@aurora-mp/cli#build": {
            "dependsOn": ["^build"],
            "outputs": ["bin/**"]
        }
    }
}
